import AppLayout from '@/layouts/AppLayout';

const OrganizationSettings = () => (
  <AppLayout title="Nucleus - Organization Settings">
    <div className="p-4">
      <h1 className="mt-2 mb-8 text-3xl font-extrabold text-gray-500">
        Organization Settings page
      </h1>
    </div>
  </AppLayout>
);

export default OrganizationSettings;
