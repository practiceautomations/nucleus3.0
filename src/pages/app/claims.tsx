import AppLayout from '@/layouts/AppLayout';

const Claims = () => (
  <AppLayout title="Nucleus - Claims">
    <div className="p-4">
      <h1 className="mt-2 mb-8 text-3xl font-extrabold text-gray-500">
        Claims page
      </h1>
    </div>
  </AppLayout>
);

export default Claims;
